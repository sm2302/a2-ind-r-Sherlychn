load("~/Downloads/19b2036.pdf")
x=9
pareto_pdf <- function(x, alpha, beta) {
# your code here
for x >= beta
pareto_pdf <- function(x, alpha, beta) {
for x >= beta
pareto_pdf <- function(x, alpha, beta) {
for x >= beta
pareto_pdf <- function(x, alpha, beta) {
for function(x) >= beta
pareto_pdf <- function(x, alpha, beta) {
x <- (alpha.beta^alpha)/(x^(alpha+1)
for x >= beta
pareto_pdf <- function(x, alpha, beta) {
x <- (alpha.beta^alpha)/(x^(alpha+1)
x >= beta
pareto_pdf <- function(x, alpha, beta) {
x <- (alpha.beta^alpha)/(x^(alpha+1)
if x >= beta
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
if ( x>=beta || x<beta ) {
}
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
if ( x>=beta || x<beta ) {
x>=beta
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
for
function(x, alpha, beta) = ((alpha)*(beta^aplha)/(x^(alpha+1)))
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
function(x, alpha, beta) = ((alpha)*(beta^aplha)/(x^(alpha+1)))
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
function(x, alpha, beta) = ((alpha)*(beta^aplha)/(x^(alpha+1)))
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
function(x, alpha, beta) -> ((alpha)*(beta^aplha)/(x^(alpha+1)))
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
function(x, alpha, beta) <- ((alpha)*(beta^aplha)/(x^(alpha+1)))
function(x, alpha, beta) <- ((alpha)*(beta^aplha)/(x^(alpha+1)))
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
function(x, alpha, beta) <- ((alpha)*(beta^aplha)/(x^(alpha+1)))
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
function(x, alpha, beta) <- ((alpha)*(beta^aplha)/(x^(alpha+1)))
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
fx <- ((alpha)*(beta^aplha)/(x^(alpha+1)))
return(fx)
}
pareto_pdf(5,10,100)
pareto_pdf(100,1000,10000)
View(pareto_pdf)
View(pareto_pdf)
pareto_pdf(5,10,100)
View(pareto_pdf)
View(pareto_pdf)
f(5)
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
fx <- ((alpha)*(beta^aplha)/(x^(alpha+1)))
return(fx)
}
View(pareto_pdf)
View(pareto_pdf)
View(pareto_pdf)
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
fx <- ((alpha)*(beta^aplha)/(x^(alpha+1)))
return(fx)
}
pareto_pdf(5,10,100)
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
dpareto <- function(x, alpha, beta){
fx <- ((alpha)*(beta^aplha)/(x^(alpha+1)))
return(fx)
}
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
dpareto <- function(x, alpha, beta){
fx <- ((alpha)*(beta^aplha)/(x^(alpha+1)))
return(fx)
}
dparetor(5,10,100)
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
dpareto <- function(x, alpha, beta){
fx <- ((alpha)*(beta^aplha)/(x^(alpha+1)))
return(fx)
}
dparetor(5,10,100)
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
pareto <- function(x) {
fx <- ((alpha)*(beta^aplha)/(x^(alpha+1)))
return(fx)
}
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
pareto <- function(x) {
fx <- ((alpha)*(beta^aplha)/(x^(alpha+1)))
return(fx)
}
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
pareto <- function(x, alpha, beta) {
fx <- ((alpha)*(beta^aplha)/(x^(alpha+1)))
return(fx)
}
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
pareto <- function(x, alpha, beta) {
fx <- ((alpha)*(beta^aplha)/(x^(alpha+1)))
pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
pareto <- function(x, alpha, beta) {
fx <- ((alpha)*(beta^aplha)/(x^(alpha+1)))
return(fx)
}
paretor(5,10,100)
}
