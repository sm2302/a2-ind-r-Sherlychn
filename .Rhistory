pareto_pdf <- function(x, alpha, beta) {
#check shape parameter and location parameter
pareto <- function(x, alpha, beta) {
fx <- ((alpha)*(beta^aplha)/(x^(alpha+1)))
return(fx)
}
paretor(5,10,100)
}
